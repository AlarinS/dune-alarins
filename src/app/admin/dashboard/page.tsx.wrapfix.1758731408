import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card"
import * as React from "react"

// Компоненты из блока (они у тебя уже сгенерированы shadcn CLI)
import { SectionCards } from "@/components/section-cards"
import { ChartAreaInteractive } from "@/components/chart-area-interactive"

// Таблица (если есть). Если что — закомментируй две строки ниже.
import { DataTable } from "@/components/data-table"
import data from "@/app/dashboard/data.json"

export default function AdminПанельPage() {
  return (
    <div className="flex flex-col gap-6">
      {/* Карточки KPI */}
      <SectionCards />

      {/* График посетителей */}
      <div className="">
            <Card className="shadow-sm">
              <CardHeader className="pb-2">
                <CardTitle>Всего посетителей</CardTitle>
                <CardDescription>Всего за последние 3 месяца</CardDescription>
              </CardHeader>
              <CardContent>
                <ChartAreaInteractive />
              </CardContent>
            </Card>
      </div>

      {/* Таблица документов/пример данных */}
      <div className="">
        <Card className="shadow-sm">
              <CardContent className="pt-6">
                <DataTable data={data as any[]} />
              </CardContent>
            </Card>
      </div>
    </div>
  )
}
